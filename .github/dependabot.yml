version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    groups:
      actions:
        patterns:
          - "*"

  - package-ecosystem: "bun"
    directory: "/"
    schedule:
      interval: "weekly"
    # Use 'increase' strategy to properly update exact pinned versions (=1.2.3 -> =1.2.4)
    versioning-strategy: increase
    groups:
      all-non-major:
        update-types:
          - "patch"
          - "minor"
    ignore:
      - dependency-name: "@types/node"
      - dependency-name: "bun-types"

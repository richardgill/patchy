amends "https://joist.fsn1.your-objectstorage.com/pkl/Pipeline.pkl"

import "../shared.pkl"

stages = new Listing<Stage> {
  new {
    name = "Typecheck"
    system = shared.main
    actions = new Listing {
      "pnpm run typecheck"
    }
  }
}